workflows:
  ios-swift-project:
    name: iOS Swift App Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    triggering:
      events:
        - push
    scripts:
      - name: Initialize Xcode project
        script: |
          echo "Building iOS App..."
